type Query {
    findAllUsers: [User]
    countUsers: Long!
    findUserById(id: String!): User
}

type Mutation {
    createUser(name: String, score: Int, level: Int) : User
}
type User {
    id: String
    name: String
    score: Int
    level: Int
}
